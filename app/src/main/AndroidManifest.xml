<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.meuapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeuApp">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".FormCadastroProfissional"/>
        <activity android:name=".FormCadastroCliente"/>
        <activity android:name=".TelaPrincipalCliente"/>
        <activity android:name=".TelaPrincipalProfissional"/>
        <activity android:name=".TelaServicosCliente"/>
        <activity android:name=".FormLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>